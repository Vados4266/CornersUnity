namespace App.Scripts
{
    public enum PlayerType : byte
    {
        None,
        Black,
        White
    }

    public enum GameMode : byte
    {
        Normal,
        Diagonal,
        VerticalHorizontal
    }
}